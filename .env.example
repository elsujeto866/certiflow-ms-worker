# Archivo de ejemplo de configuración de variables de entorno
# Copia este archivo como .env y configura los valores apropiados

# =============================================================================
# CONFIGURACIÓN DE OPENAI
# =============================================================================
# Tu clave de API de OpenAI (requerida)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Modelo de OpenAI a usar (opcional, por defecto: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Máximo de tokens para respuestas de OpenAI (opcional, por defecto: 4000)
OPENAI_MAX_TOKENS=4000

# =============================================================================
# CONFIGURACIÓN DE LA APLICACIÓN
# =============================================================================
# Nombre de la aplicación (opcional)
APP_NAME=Certiflow API Worker

# Versión (opcional)
VERSION=1.0.0

# Modo debug (opcional, por defecto: False)
DEBUG=False

# =============================================================================
# CONFIGURACIÓN DEL SERVIDOR
# =============================================================================
# Host del servidor (opcional, por defecto: 127.0.0.1)
HOST=127.0.0.1

# Puerto del servidor (opcional, por defecto: 8000)
PORT=8000

# =============================================================================
# CONFIGURACIÓN DE ARCHIVOS
# =============================================================================
# Tamaño máximo de archivo en bytes (opcional, por defecto: 10MB)
MAX_FILE_SIZE=10485760

# Directorio de uploads (opcional, por defecto: uploads)
UPLOAD_DIR=uploads

# Directorio de plantillas (opcional, por defecto: templates)
TEMPLATES_DIR=templates

# =============================================================================
# EJEMPLO DE CONFIGURACIÓN COMPLETA MÍNIMA
# =============================================================================
# Para usar la aplicación, solo necesitas configurar:
# OPENAI_API_KEY=tu_clave_aqui
# 
# El resto de valores tienen defaults apropiados para desarrollo.